<!-- Project Ambrose by Imjustchico: The toaster from shadcn-svelte on svelte-sonner, copied in and owned: it follows the panel's own light and dark choice and draws each toast from the panel stylesheet's theme variables. -->
<script lang="ts">
    import { Toaster as Sonner, type ToasterProps as SonnerProps } from "svelte-sonner";
    import Loader2Icon from "@lucide/svelte/icons/loader-2";
    import CircleCheckIcon from "@lucide/svelte/icons/circle-check";
    import OctagonXIcon from "@lucide/svelte/icons/octagon-x";
    import InfoIcon from "@lucide/svelte/icons/info";
    import TriangleAlertIcon from "@lucide/svelte/icons/triangle-alert";
    import { theme } from "$lib/theme.svelte.js";

    let { ...restProps }: SonnerProps = $props();
</script>

<Sonner
    theme={theme.resolved}
    class="toaster group"
    style="--normal-bg: var(--color-popover); --normal-text: var(--color-popover-foreground); --normal-border: var(--color-border);"
    toastOptions={{
        classes: {
            toast: "cn-toast",
            description: "text-muted-foreground!",
        },
    }}
    {...restProps}
>
    {#snippet loadingIcon()}
        <Loader2Icon class="size-4 animate-spin" />
    {/snippet}
    {#snippet successIcon()}
        <CircleCheckIcon class="size-4" />
    {/snippet}
    {#snippet errorIcon()}
        <OctagonXIcon class="size-4" />
    {/snippet}
    {#snippet infoIcon()}
        <InfoIcon class="size-4" />
    {/snippet}
    {#snippet warningIcon()}
        <TriangleAlertIcon class="size-4" />
    {/snippet}
</Sonner>
